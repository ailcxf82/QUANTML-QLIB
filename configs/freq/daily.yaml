# ???day????????????????????
# ? base.yaml + model/*.yaml ???????????

freq_meta:
  name: "daily"
  freq_key: "1day"

# ?? base.yaml ? dataset.kwargs.segments
# valid ?? 2024 ?????????? + ??????????
#   ???????????????????? ICIR ?????????
# ?????????train ?? 6 ???valid ?? 6 ????
dataset_segments:
  train: ["2020-01-01", "2023-12-31"]
  valid: ["2024-01-01", "2024-12-31"]
  test:  ["2025-01-01", "2026-05-12"]

executor:
  class: "SimulatorExecutor"
  module_path: "qlib.backtest.executor"
  kwargs:
    time_per_step: "day"
    generate_portfolio_metrics: true

backtest:
  exchange_kwargs:
    freq: "day"
    # ?9.5% ????????????????A???? ?10%?
    # ??ST ????? ?5%???? universe=csi500 ?? ST???? 0.095
    limit_threshold: 0.095
    deal_price: "close"
    # ?????????/???????????? + ??????????
    #   ?? = ?? 0.05% + ???? 0.10% + ???? 0.10% = 0.25%
    #   ?? = ?? 0.05% + ??? 0.10% + ???? 0.10% + ???? 0.10% = 0.35%
    open_cost: 0.0025
    close_cost: 0.0035
    # ?????? 5 ?
    min_cost: 5
    # ?? impact_cost ??? 0?qlib ??????????
    #     adj_cost_ratio = impact_cost * (trade_val / total_trade_val)^2
    # ??? qlib ?????? bug ?? $volume ????????????
    # ?????? 2025-01-02 ???? 1.38 ???qlib ?? 1.38??
    # ? _calc_trade_info_by_order ?? `volume * price` ?????
    # ?? (trade_val / total_trade_val)^2 ????????
    # ???account=30 ??impact_cost=0.001 ? 100% ??? deal_amount=0 ???
    # ?? 0.10% ?????????? open_cost/close_cost??????????
    impact_cost: 0
    # ??A? 100 ?????? qlib adjusted_price ?????????
    #   ?????? "trade unit 100 is not supported in adjusted_price mode"?
    #   ?? universe ? csi500 ?????????????????
