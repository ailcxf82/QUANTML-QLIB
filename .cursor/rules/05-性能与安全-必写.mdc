---
description: 量化训练与回测工程的性能与安全硬约束
alwaysApply: true
---

# 性能与安全（必写）

- 高频场景默认向量化与批处理，禁止在热路径中逐行 Python 循环处理行情数据。
- 数据访问需分层缓存（内存/磁盘），并设置 TTL 与失效策略，避免重复加载全量历史数据。
- 训练任务应支持增量更新与并行执行；长任务必须记录阶段耗时与资源使用。
- 禁止将密钥、账号、券商配置写入代码仓库；统一读取环境变量或密钥管理服务。
- 输入数据必须校验：时间戳单调、字段完整性、价格与成交量非负、异常值处理有记录。
- 回测与仿真执行需有风控护栏：仓位上限、行业暴露、单票权重、最大回撤熔断。
- 对外接口最小权限原则：只暴露必要读写能力，所有关键操作记录审计日志。
