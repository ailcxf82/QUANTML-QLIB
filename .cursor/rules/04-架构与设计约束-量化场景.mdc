---
description: 高频量化选股系统的架构与设计约束
alwaysApply: true
---

# 架构与设计约束（量化场景）

- 采用分层架构：`Data -> Feature -> Model -> Signal -> Portfolio -> Backtest -> Evaluation`。
- 必须防止未来函数与数据泄漏：时间切分先于标准化、特征选择与标签构建。
- 训练与推理分离：训练代码不得复用实时状态；推理服务不得依赖训练期对象缓存。
- 信号生成必须包含置信度或打分，交易模块只消费标准化信号接口。
- 回测模块必须显式建模交易成本：手续费、滑点、冲击成本、最小成交单位、涨跌停限制。
- 策略评估至少输出：年化收益、最大回撤、Sharpe、信息比率、换手率、胜率、容量指标。
- 所有实验要求可复现：固定随机种子、配置快照、数据版本、代码版本、模型版本。
